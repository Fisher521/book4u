# 逢书 · 推荐策略设计文档

> 此刻，与这本书不期而遇。
>
> 这份文档记录的是 **2026-05-03 设计冻结的基线版本**，未来调整请先理解这里的取舍再动 prompt。

---

## 1 · 产品定位

**逢书** 是一个根据用户当下的画像（MBTI + 人生阶段）和此刻的状态（文字 + 图片）推荐 **5 本"想读"的书**的工具。

**核心北极星：想读，不是应该读。**

> "应该读" = 理性认可、补全维度、扩展视野
> "想读" = 看一眼就被勾住、像在等你、读第一段就停不下来

我们只推第二种。

---

## 2 · 输入 Schema

| 字段 | 必填 | 说明 |
|---|---|---|
| `mbti` | ✅ | 16 种类型之一 |
| `age` | ✅ | 5 段人生阶段（18-25 / 26-30 / 31-35 / 36-45 / 46-55 / 56+）|
| `expression` | ⚠ 二选一 | 自由文本：情绪 / 想法 / 在听的歌 / 在看的视频 / 在纠结的事 |
| `images` | ⚠ 二选一 | 0-6 张图：朋友圈截图 / 听歌列表 / 短视频截图 / 随手拍 |

**`expression` + `images` 至少有一个非空。**

砍掉过的输入：性别 / 性取向 / 偏好 / 星座 / 单独的"情绪/音乐/想法"三栏（已合并成 `expression`）。

---

## 3 · 推荐策略 — 3+1+1 比例

### 5 本的角色分工

```
共鸣 #1 【陪伴】   ── 接情绪，文学性强，不教导
共鸣 #2 【陪伴】   ── 与 #1 不同角度，仍是陪伴
共鸣 #3 【桥梁】   ── 思想密度 + 文学性，接白天理性轨道
破茧 #1 【源头破】 ── 二手概念 → 第一手震撼
破茧 #2 【文化/体裁破】── 陌生语境 / 罕见写法
```

### 关键设计决策

**为什么不强制"非 humanities"？**
之前硬规则导致模型推 Norman 设计心理学、海洋生物学这种"功能教科书"。新规则改成 **桥梁书优先文学性**——可以是哲学 / 心理 / 思想，但**绝不能是 self-help / 商业管理 / 教科书**。

**为什么破茧拆成"源头破" + "文化/体裁破"？**
原来"硬破"+"软破"把破茧推向"立场对冲"（如《贫穷的本质》塞给敏感个体），变成"理性教导"。新规则更精准：

- **源头破**：用户在视频/二手解读里消化的概念 → 推第一手源头（如缓存 Jung 视频 → 推荣格自传）
- **文化/体裁破**：陌生语境/罕见写法（非英语主流 / 实验体裁）

**为什么深读放在 prompt 第一位？**
模型默认回应表面那个 ta（"焦虑"→ 推焦虑书）。但人不是来要"焦虑解决方案"的。Prompt 强制模型在内心先做：
1. 哪些表达是自我合理化？
2. 哪些选择是回避动作？
3. ta 把哪些深度藏起来了？为什么藏？
4. ta 在亲密关系/工作/自我之间，哪一组关系最紧张？

然后**回应深层那个 ta，不是表面那个**。

---

## 4 · 校验策略 — Option C（重试至成功）

### 流程

```
推荐 5 本 → 并行 douban 校验
    ↓
有 not_found 或 mismatch？
    ├─ 否 → 完成
    └─ 是 → 重试：告诉模型哪些没通过 + 已通过的别再推 + 给替换书
              ↓
            最多 2 轮重试
```

### 校验状态

| status | 含义 | UI |
|---|---|---|
| `ok` | 找到且作者吻合 | ✓ 已与豆瓣对上（用真实链接 + 封面）|
| `mismatch` | 找到但作者疑似不符 | ⚠ 似有偏差（用搜索链接，不显示封面）|
| `not_found` | 两轮搜索都空 | ⚠ 豆瓣无寻 |
| `error` | 网络/超时 | （静默）|

**数据源：** `https://book.douban.com/j/subject_suggest?q={query}`（公开 JSON 接口，无需 token）

---

## 5 · 黑名单（绝对不允许出现在共鸣里）

### 自助 / 心理类
被讨厌的勇气、Hollis《中年之路》、《菊与刀》、《非暴力沟通》、《自卑与超越》

### 神级小说
百年孤独、活着、史铁生《我与地坛》、小王子、月亮与六便士、村上春树《挪威的森林》《海边的卡夫卡》《1Q84》、马尔克斯、卡尔维诺《看不见的城市》、毛姆《刀锋》《人性的枷锁》

### 文艺青年滥推
罗兰·巴特《恋人絮语》、安妮·埃尔诺《年月》《悠悠岁月》、约翰·伯格《观看之道》、辛波斯卡《万物静默如谜》、韩炳哲《倦怠社会》、加缪《局外人》《西西弗神话》、茨威格《人类群星闪耀时》《一个陌生女人的来信》、黑塞《悉达多》《荒原狼》、昆德拉《不能承受的生命之轻》

### 流行治愈系（绝不能进破茧）
巴克曼系列（《欧维》《外婆的道歉信》《焦虑的人》）、《岛上书店》、《追风筝的人》《灿烂千阳》、《偷影子的人》、阿连德《幽灵之家》、《岛屿生活课》、东野圭吾《解忧杂货店》《白夜行》

### 国内青春文学
郭敬明全部（《悲伤逆流成河》《小时代》等）、安妮宝贝/庆山、张嘉佳《从你的全世界路过》、刘同《谁的青春不迷茫》

### 扩展类畅销书
人类简史、未来简史、原子习惯、纳瓦尔宝典、《如何阅读一本书》、《思考，快与慢》

---

## 6 · MBTI 启发表

每一型对应一段"隐秘需求"描述。模型基于这段调整推荐口径——不是给标签贴书，是给模式回应。

完整内容见 `src/app/api/recommend/route.ts` 的 `MBTI_HINTS`。

例（INFJ）：
> 深度敏感 + 理想主义 + 怕被定义。怕"被看穿但被误解"。需要"被精准接住但不被框定"的书。容易自我消耗，不要再推"更深刻"的——推让 ta 学会接受自己复杂性的、温和但有重量的同伴书。

---

## 7 · 人生阶段启发表

5 段：
- 18-25 探索期
- 26-30 立业期
- 31-35 重塑期
- 36-45 中年门槛
- 46-55 第二春
- 56+ 整合期

完整内容见 `AGE_HINTS`。

例（46-55 第二春）：
> 孩子离巢、事业天花板、身体信号变化。需要帮 ta 重新出发或安然停留的书。深度但不沉重，可以推哲学但要有温度。

---

## 8 · 技术架构

```
[用户表单] (Next.js 16 App Router + Tailwind v4)
    ↓ POST /api/recommend (FormData)
[路由] src/app/api/recommend/route.ts
    ├── buildSystemPrompt({ mbtiHint, ageHint })  ← 动态拼装
    ├── callLLM(system + user + images)            ← src/lib/llm.ts
    │     ├── qwen-vl-max（默认，DashScope OpenAI 兼容）
    │     └── claude-sonnet-4-6（备选，Agent SDK）
    ├── verifyMany(books)                          ← src/lib/douban.ts
    │     └── 豆瓣 subject_suggest JSON
    └── 重试循环（最多 2 轮）
    ↓
[响应] 5 本带 douban 字段的书 + _meta { cost_usd, model, retry_rounds, unverified_count }
    ↓
[前端] BookCard 渲染：封面 + 钩子 + 推荐理由 + 校验徽章 + 三方链接
```

### 模型切换
`.env.local` 设置 `MODEL=qwen-vl-max` 或 `MODEL=claude-sonnet-4-6` 切换。

### 成本对比
- Qwen-VL-Max：单次约 $0.02（走 DashScope 计费）
- Claude Sonnet 4.6：单次约 $0.20（走 Anthropic Max 月度额度）

---

## 8.5 · 编书的进化与对策

模型的"编书"会在不同维度反复出现，每堵一处它从下一处冒出：

| 阶段 | 编什么 | 对策 |
|---|---|---|
| 初版 | 编书名 + 编内容 | 豆瓣校验 + 重试（已上线）|
| 第二版 | 不编书名，但编 `original_title` 和 `translator`（豆瓣校验不覆盖元数据） | **从 schema 删除这两个字段**（已上线 2026-05-03 晚）|
| 未来可能 | 编 author_note 国籍/年代 | 暂不处理；豆瓣链接一点就有正确信息 |

**原则：信息字段只保留豆瓣能验证的 + 模型能稳定输出的。** 元数据交给真实链接兜底。

---

## 9 · 已知不做的事 / 反模式备忘

- ❌ 不做用户系统、登录、历史记录持久化（本地 localStorage 够了）
- ❌ 不做"性别 / 性取向"输入（用 expression free text 让用户自主表达偏好）
- ❌ 不做"豆瓣评分人数 < N 才算冷门"硬过滤（黑名单 + 模型自觉够了）
- ❌ 不做多步推理（候选池 → ranking）—— 单次 LLM + 校验已经够好
- ❌ 不做反馈学习 / 已读过滤（自己用，记忆量不大）
- ❌ 不做候选共鸣/破茧的二选一让用户挑（5 本一次性出，按 3+1+1 角色分工）

---

## 10 · 未来增强 TODO

按优先级：

1. **图片压缩**（客户端 canvas resize → 1568px max + JPEG 0.85）—— 减少上传 + Qwen vision 处理时间
2. **流式进度**（SSE 把"已推荐 / 正在校验 / 替换中"的状态实时推到前端）—— 改善 60-180s 等待体感
3. **MBTI / 年纪启发表持续打磨**（每用一次，记录命中或错位，迭代条目）
4. **黑名单逐步扩展**（每次出现"应该黑但没黑"的书，加进去）
5. **可选画像维度**（如：阅读速度、最近读过的 2-3 本——但**不要靠收集累积，靠每次重新声明**）

---

## 11 · 设计原则（如果有人要继承这个项目）

1. **想读 > 应该读**——每个推荐过一次"看到书名 + 钩子句会不会想立刻翻开第一页"测试
2. **陪伴 > 教导**——回应深层那个 ta，不是表面那个 ta
3. **冷门 > 神坛**——黑名单是反推动力，让模型不偷懒
4. **3+1+1 角色分工**——结构上保证既感性又理性
5. **真书 > 完美书**——豆瓣校验 + 重试拦截编书
6. **少加约束 > 加更多约束**——每加一条硬规则，都要预判它会把模型逼向哪种"安全偷懒"
7. **不功能化 UI**——文艺感不是装饰，是匹配产品定位（书 = 慢 = 沉淀）

---

_最后更新 2026-05-03，由 Claude Code 协作完成。_
